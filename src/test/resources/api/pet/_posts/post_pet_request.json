{
  "id": "${petId}",
  "category": {
    "id": "${petId}",
    "name": "${petCategory}"
  },
  "name": "${petName}",
  "photoUrls": [
    "${petPhotoUrls}"
  ],
  "tags": [
    {
      "id": "${petId}",
      "name": "${petTags}"
    }
  ],
  "status": "${petStatus}"
}